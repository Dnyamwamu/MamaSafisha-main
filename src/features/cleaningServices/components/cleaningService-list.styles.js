import styled from 'styled-components/native'
import { FlatList } from 'react-native'

export const CleaningServiceList = styled(FlatList).attrs({
  contentContainerStyle: {
    padding: 16,
  },
})``
